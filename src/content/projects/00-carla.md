---
title: CARLA Autonomous Driving Simulator
summary: TODO — one sentence describing what you are building in CARLA. This is the line shown on the banner and the Home slide.
image: /videos/projects/carla-demo.mp4
# github: https://github.com/Sidharth-82/your-carla-repo
tags: [Python, CARLA, Simulation, Computer Vision, PyTorch]
featured: true
spotlight: true
status: In Progress
order: 0
---

<!--
═══════════════════════════════════════════════════════════════════════════
SCAFFOLD — everything below is placeholder. Replace it with your own write-up.
═══════════════════════════════════════════════════════════════════════════

SPOTLIGHT: `spotlight: true` is what makes this the in-progress hero project —
a full-width banner above the grid on /projects, plus its own "Currently
Building" slide on Home (right after Future Goals). Only ONE project should set
it; if several do, the lowest `order` wins and the rest render as normal tiles.
`status:` is the pulsing badge text — change it to "Paused", "Shipped", etc.
Delete both fields to demote this back to an ordinary tile.

VIDEO: `image:` points at /videos/projects/carla-demo.mp4, which does not exist
yet — drop the clip in `public/videos/projects/` next to MTE380-Robot.mp4.
Until then the tile shows an empty black box. The banner and grid tiles play it
on hover, the Home slide autoplays it muted, and the popup shows it with native
controls. Any .mp4/.webm/.mov works — or point `image:` at a still
(.png/.jpg/.svg) instead.

MORE MEDIA IN THE WRITE-UP: normal markdown images work and are click-to-zoom
— `![BEV output](/images/projects/carla-bev.png)`. For extra videos use raw
HTML: `<video src="/videos/projects/carla-lidar.mp4" controls playsinline></video>`.
All paths are root-relative to /public.

TAGS: any tag matching a skill's `name` or `aliases` becomes a clickable chip
in this popup and makes the project show up on that skill's page. "CARLA" has
no skill yet — add it to the `aliases` of src/content/skills/15-simulation.md
if you want the chip to link there.
-->

**TODO: open with what this project is and why you are building it.** One
paragraph: what CARLA gives you (a photorealistic Unreal-based driving
simulator with a scriptable sensor suite and repeatable traffic scenarios),
and what you are using it for.

## Goals

- TODO — the concrete thing you want working first.
- TODO — the next milestone after that.
- TODO — the stretch goal.

## Progress

- TODO — what is actually running today.
- TODO — what is half-built.

## What I'm learning

TODO — the parts that turned out harder than expected, and what you would do
differently. This section is what makes the tile worth clicking.
